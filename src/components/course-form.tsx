import { saveCourse } from '@/app/(dashboard)/admin/actions';
type Course={id?:string;title?:string;slug?:string;short_description?:string|null;description?:string|null;level?:string;price_xof?:number;duration_minutes?:number;status?:string};
export function CourseForm({course={}}:{course?:Course}){const field='w-full rounded-xl border border-slate-300 px-4 py-3 focus:border-[#b98722] focus:outline-none';return <form action={saveCourse} className="mt-8 grid gap-5 rounded-3xl bg-white p-6 shadow-sm">{course.id&&<input type="hidden" name="id" value={course.id}/>}<label className="font-bold">Titre<input required name="title" defaultValue={course.title} className={field}/></label><label className="font-bold">Slug<input required name="slug" defaultValue={course.slug} pattern="[a-z0-9]+(?:-[a-z0-9]+)*" className={field}/></label><label className="font-bold">Résumé<textarea required name="short_description" defaultValue={course.short_description??''} className={field}/></label><label className="font-bold">Description<textarea required rows={6} name="description" defaultValue={course.description??''} className={field}/></label><div className="grid gap-4 sm:grid-cols-3"><label className="font-bold">Niveau<select name="level" defaultValue={course.level??'Débutant'} className={field}><option>Débutant</option><option>Intermédiaire</option><option>Avancé</option></select></label><label className="font-bold">Prix FCFA<input type="number" min="0" step="5" name="price_xof" defaultValue={course.price_xof??0} className={field}/></label><label className="font-bold">Durée (minutes)<input type="number" min="0" name="duration_minutes" defaultValue={course.duration_minutes??0} className={field}/></label></div><label className="font-bold">Statut<select name="status" defaultValue={course.status??'draft'} className={field}><option value="draft">Brouillon</option><option value="published">Publiée</option><option value="archived">Archivée</option></select></label><button className="rounded-xl bg-green-700 px-6 py-4 font-extrabold text-white">Enregistrer la formation</button></form>}
