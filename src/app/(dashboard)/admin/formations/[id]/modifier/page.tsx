import { notFound } from 'next/navigation';import { AdminShell } from '@/components/admin-nav';import { CourseForm } from '@/components/course-form';import { requireAdmin } from '@/lib/auth/authorization';
export default async function EditCourse({params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase}=await requireAdmin(`/admin/formations/${id}/modifier`);const {data}=await supabase.from('courses').select('*').eq('id',id).maybeSingle();if(!data)notFound();return <AdminShell><h1 className="text-4xl font-extrabold text-[#071b3a]">Modifier la formation</h1><CourseForm course={data}/></AdminShell>}
