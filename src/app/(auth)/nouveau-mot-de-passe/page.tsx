import { updatePassword } from '@/app/(auth)/actions';import { PublicHeader } from '@/components/brand';
export default async function NewPassword({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="min-h-screen bg-slate-50"><PublicHeader/><section className="mx-auto max-w-lg px-4 py-16"><form action={updatePassword} className="rounded-3xl bg-white p-8 shadow-sm"><h1 className="text-3xl font-extrabold text-[#071b3a]">Nouveau mot de passe</h1>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-red-800">{error}</p>}<label className="mt-6 block font-bold">Mot de passe<input required minLength={8} type="password" name="password" className="mt-2 w-full rounded-xl border px-4 py-3"/></label><button className="mt-5 w-full rounded-xl bg-green-700 px-5 py-3 font-bold text-white">Mettre à jour</button></form></section></main>}
