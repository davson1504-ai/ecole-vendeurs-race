import {describe,expect,it} from 'vitest';import {GET as webhookGet,POST as webhookPost} from './webhooks/cinetpay/route';import {GET as simulationGet,POST as simulationPost} from './paiement/simulation/route';
describe('routes paiement neutralisées',()=>{it('désactive le webhook GET',async()=>expect((await webhookGet()).status).toBe(503));it('désactive le webhook POST',async()=>expect((await webhookPost()).status).toBe(503));it('refuse la simulation GET',async()=>expect((await simulationGet()).status).toBe(405));it('désactive la simulation POST',async()=>expect((await simulationPost()).status).toBe(503));});
